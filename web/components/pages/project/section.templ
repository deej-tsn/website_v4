package components

import "github.com/deej-tsn/website_v4/internal/models"

templ Section(title string, projects []models.ProjectHero) {
    <div>
        <h2>{title}</h2>
        for _, project := range projects {
            @Hero(project)
        }
    </div>
}